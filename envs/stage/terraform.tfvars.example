# STAGE Environment: 예시 변수 파일
# 이 파일을 terraform.tfvars로 복사하고 실제 값을 입력하세요.
# 주의: terraform.tfvars는 .gitignore에 추가하여 Git에 커밋하지 마세요.

# AWS 리전
aws_region = "ap-southeast-2"

# AWS 계정 ID (필수)
aws_account_id = "123456789012"

# 프로젝트 설정
project_name = "kyeol"
owner_prefix = "min"
environment  = "stage"

# VPC CIDR (spec.md 기준 - STAGE)
vpc_cidr = "10.20.0.0/16"

# 가용영역 (Sydney)
azs = ["ap-southeast-2a", "ap-southeast-2c"]

# EKS 설정 (STAGE: DEV보다 상향)
eks_cluster_version     = "1.29"
eks_node_instance_types = ["t3.large"]
eks_node_desired_size   = 3
eks_node_min_size       = 2
eks_node_max_size       = 5

# RDS 설정 (STAGE: Multi-AZ 활성)
rds_instance_class    = "db.t3.medium"
rds_multi_az          = true
rds_allocated_storage = 50

# Route53 Hosted Zone ID (msp-g1.click)
hosted_zone_id = "Z0XXXXXXXXXXXX"

# Cache 설정 (STAGE: 활성화)
enable_cache    = true
cache_node_type = "cache.t3.small"
