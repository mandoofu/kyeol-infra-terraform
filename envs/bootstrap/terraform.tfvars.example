# Bootstrap: 예시 변수 파일
# 이 파일을 terraform.tfvars로 복사하고 실제 값을 입력하세요.
# 주의: terraform.tfvars는 .gitignore에 추가하여 Git에 커밋하지 마세요.

# AWS 리전 (기본: ap-southeast-2)
aws_region = "ap-southeast-2"

# AWS 계정 ID (필수)
# S3 버킷 네이밍에 사용됩니다.
aws_account_id = "123456789012"

# 프로젝트 이름 (기본: test)
project_name = "test"

# 소유자 프리픽스 (기본: test)
owner_prefix = "test"

# KMS 암호화 활성화 (기본: true)
# 비용 절감을 위해 false로 설정 가능 (AES256 사용)
enable_kms_encryption = true
